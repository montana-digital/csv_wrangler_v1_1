[pytest]
# Pytest configuration for UI tests
testpaths = src/tests/e2e
python_files = test_ui_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --tb=short
    -v
markers =
    ui: UI tests using Playwright
    e2e: End-to-end tests

